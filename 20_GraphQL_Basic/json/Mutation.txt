mutation insertDetail($id_anggota: Int!, $nilai: Int!, $pelajaran: String!, $status: Boolean!){
    insert_graphqlbasic_keterangan_one(object: {id_anggota: $id_anggota, nilai: $nilai, pelajaran: $pelajaran, status: $status}) {
        id_anggota
        nilai
        pelajaran
        status
        anggota {
            nama
            id
        }
    }
}

mutation updateTrue {
    update_graphqlbasic_keterangan_many(updates: {where: {status: {_eq: false}}, _set: {status: true}}) {
        returning {
            id
            id_anggota
            nilai
            pelajaran
            status
        }
    }
}